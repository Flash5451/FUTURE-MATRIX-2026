// Animated PCB-trace overlay intentionally removed for a calmer, formal hero —
// kept as a no-op component so existing <CircuitTrace /> usages don't need touching.
export default function CircuitTrace() {
  return null;
}
